# 2026-05-02

## 오늘 학습
- GitHub 계정 생성 및 환경 구축
- VS Code, GitHub Desktop 사용법 익힘
- 3개 레포 publish (TIL, algorithm-practice, 프로필)
- SQL 4문제 풀이 (SELECT, SUM/AVG, JOIN, String/Date)
- Python Lv0 3문제 풀이

## 배운 점

### SQL
- ROUND 함수 — 소수 자리 반올림
- AVG, JOIN, 서브쿼리
- ORDER BY 다중 컬럼 정렬
- YEAR() — 날짜에서 연도 추출
- 정확 일치는 `=`, 패턴 매칭은 `LIKE`

### Python
- range vs while 차이
- 복합 연산자 //=
- len(), str() 형변환
- 슬라이싱 [-4:]
- elif는 else if의 줄임
- 리스트 인덱스는 0부터, 사람의 순서는 1부터

### Git / GitHub
- add → commit → push 흐름
- 카테고리별 폴더 구조
- .sql/.py + .md 두 파일 세트
- commit 메시지 일관된 형식

## 막힌 부분
- range(1)을 무한 반복으로 오해
- Python에 SQL의 LIKE 같은 와일드카드 없음
- 리스트 인덱스에 문자열 사용 시도
- 함수 중첩 시 괄호 위치 실수

## 내일 할 것
- 일요일 휴식
- 월요일부터 SQL 1~2문제 + Python 1문제